name: Release Charm to Edge and Publish Libraries

on:
  push:
    paths-ignore:
      - '.gitignore'
      - 'LICENSE'
      - '**.md'
    branches:
      - main
  pull_request:
    branch:
      - main
    secret:
      CHARMHUB_TOKEN:
        default: ${{ secrets.CHARMHUB_TOKEN }}

jobs:
  release:
    uses: canonical/observability/.github/workflows/charm-release.yaml@v0
    secrets:
      CHARMHUB_TOKEN: ${{ secrets.CHARMHUB_TOKEN }}
